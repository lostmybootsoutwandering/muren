# gcloud app deploy --project lostmyboots --version 2

runtime: python313

handlers:
  - url: /
    static_files: static/index.html
    upload: static/index.html

  - url: /(.*)
    static_files: static/\1
    upload: static/(.*)
